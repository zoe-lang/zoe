#(?
  (var (= b
      (fndef (lst) 2)
  ))
#)
var b = fn () => 2

#(?

  (var (= b
    (fndef
      (proto
        (lst (var (":" a String)))
        String
      )
      (block
        (return a)
      )
    )
  ))

#)
var b = fn (a: String) -> String => { return a }
